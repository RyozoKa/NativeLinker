/*
Base class for everything happening in the engine.
Theoretically script objects and native objects exists in paralell 
Revision:
	*Christopher Békési - initial build
*/
#pragma once
class DLLEXPORT HypObject
{
	//We need methods to register an object within the engine.
	//HypObject* operator=(HypObject*);
};