//Class modifiers:
// Static
// Abstract
// NoInit (Use with caution)
// Native
//THECLASS(class,name,bDelete)
public native abstract class Object 
{	
	Class<Object> Thisclass;
	//bool 	bT1;
	byte 	bT2;
	short 	T3;
	int 	T4;
	int 	T5;
	String 	name;
	String 	Name2;
	bool bDelete;
	Test T;
	
	native static void Log(String);
	
	void Test(bool Testb,unsigned int Asd)
	{
		int i;
		String S = "CoolString", SS = "Whatever";
		//A(100, "HelloString",B("COOL"));
		//(A());
		//((A()));
		
		
	//	A;
		if(A)
		{
			(--A && B.C++) && 0b10101 + 200 + 300 > 100 + 100 && C && D ;
			A && C().D.E && A + D.C.E[i] > C.F.F;
			A.B && C.D.E && A + D.C.E > C.F.F;
			A.B.C[i] && C.D.E && A + D.C.E > C.F.F;
			(7 * 1) + (7 * 3);
			A = B = 10 + 10 + 500;
			A && B && C && D && E;
		}
	
		else
			A[i] && B && C && D[i];
	
		for(int i=0;A && B;i++)
		{
			(7 * 1) + (7 * 3);
			A = B = 10 + 10 + 500;
			A && B && C && D && E;
		}
		do
		{
			(7 * 1) + (7 * 3);
			A = B = 10 + 10 + 500;
			A && B && C && D && bool(E);
		}
		while( A && !(A && B))
	
		A = Object(new B());
	}
	static operator int ++(int i);

};

native abstract class Test extends Object{
	byte A;
	short B;
	int C;
	bool D;
	bool DA;
	bool DB;
	int64 E;
	unsigned int64 F;
	Object O;
	
	void CC();
	native void GG();
	void Test(bool Testb,unsigned int Asd);
};